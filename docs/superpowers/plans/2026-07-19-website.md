# Avocat Bianca Rohozneanu Website Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a bilingual (RO/EN) one-page promotional website for lawyer Bianca Rohozneanu (Cluj-Napoca) and publish it via GitHub + Netlify.

**Architecture:** Two static HTML pages (`index.html` Romanian, `en/index.html` English) sharing one stylesheet. No build step, no JavaScript. Fonts served locally. Contact form handled by Netlify Forms.

**Tech Stack:** HTML5, CSS3, Netlify (hosting + forms), GitHub (repo `alexrohozneanu/avocat-bianca-rohozneanu`).

## Global Constraints

- Spec: `docs/superpowers/specs/2026-07-19-website-design.md` — read it before starting.
- Project root: `/Users/alexr/publicrepos/avocat-bianca-rohozneanu`
- No frameworks, no build tools, no JavaScript.
- All placeholder content MUST be marked with an HTML comment `<!-- PROVIZORIU -->` on the line above it.
- Colors: navy `#1c2b4a`, cream `#faf7f2`, gold `#b08d3e`, body text `#2a2a2a`.
- Fonts: Playfair Display 700 (headings), Source Sans 3 400/600 (body), served locally from `fonts/`, latin + latin-ext subsets (Romanian diacritics ă â î ș ț MUST render in the heading font).
- Romanian page is the default at `/`; English at `/en/`.
- Verification server: `python3 -m http.server 8321` run from the project root (kill it when the task's verification is done).

---

### Task 1: Scaffolding and local fonts

**Files:**
- Create: `.gitignore`
- Create: `fonts/playfair-display-700-latin.woff2`, `fonts/playfair-display-700-latin-ext.woff2`
- Create: `fonts/source-sans-3-400-latin.woff2`, `fonts/source-sans-3-400-latin-ext.woff2`
- Create: `fonts/source-sans-3-600-latin.woff2`, `fonts/source-sans-3-600-latin-ext.woff2`

**Interfaces:**
- Consumes: nothing (first task)
- Produces: the six `.woff2` files above, at exactly those paths — Task 2's `@font-face` rules reference them as `../fonts/<name>.woff2`.

- [ ] **Step 1: Create .gitignore**

```gitignore
.DS_Store
```

- [ ] **Step 2: Download the six font files from the Fontsource CDN**

```bash
cd /Users/alexr/publicrepos/avocat-bianca-rohozneanu
mkdir -p fonts
curl -fsSL -o fonts/playfair-display-700-latin.woff2     "https://cdn.jsdelivr.net/fontsource/fonts/playfair-display@latest/latin-700-normal.woff2"
curl -fsSL -o fonts/playfair-display-700-latin-ext.woff2 "https://cdn.jsdelivr.net/fontsource/fonts/playfair-display@latest/latin-ext-700-normal.woff2"
curl -fsSL -o fonts/source-sans-3-400-latin.woff2        "https://cdn.jsdelivr.net/fontsource/fonts/source-sans-3@latest/latin-400-normal.woff2"
curl -fsSL -o fonts/source-sans-3-400-latin-ext.woff2    "https://cdn.jsdelivr.net/fontsource/fonts/source-sans-3@latest/latin-ext-400-normal.woff2"
curl -fsSL -o fonts/source-sans-3-600-latin.woff2        "https://cdn.jsdelivr.net/fontsource/fonts/source-sans-3@latest/latin-600-normal.woff2"
curl -fsSL -o fonts/source-sans-3-600-latin-ext.woff2    "https://cdn.jsdelivr.net/fontsource/fonts/source-sans-3@latest/latin-ext-600-normal.woff2"
```

Fallback if the CDN is unreachable: STOP and report — do not substitute remote font links; the site must not call external hosts.

- [ ] **Step 3: Verify all six files are real woff2 (not error pages)**

Run:
```bash
cd /Users/alexr/publicrepos/avocat-bianca-rohozneanu
for f in fonts/*.woff2; do printf '%s: ' "$f"; head -c4 "$f" | od -c | head -1; done
```
Expected: every line shows `w   O   F   2` as the first four bytes. Also `ls -la fonts/` — every file > 5 KB.

- [ ] **Step 4: Commit**

```bash
git add .gitignore fonts
git commit -m "feat: add scaffolding and local webfonts (Playfair Display, Source Sans 3)"
```

---

### Task 2: Shared stylesheet

**Files:**
- Create: `css/style.css`

**Interfaces:**
- Consumes: font files from Task 1 (referenced as `../fonts/…`).
- Produces: `css/style.css` with the exact class names used by Tasks 3–4: `.site-header`, `.nav`, `.nav-links`, `.lang-switch`, `.hero`, `.hero-inner`, `.btn`, `.section`, `.section--navy`, `.section-title`, `.about-grid`, `.portrait`, `.cards`, `.card`, `.contact-grid`, `.contact-form`, `.contact-info`, `.site-footer`.

- [ ] **Step 1: Write the complete stylesheet**

```css
/* Avocat Bianca Rohozneanu — shared stylesheet (RO + EN pages) */

@font-face {
  font-family: "Playfair Display";
  font-weight: 700;
  font-display: swap;
  src: url("../fonts/playfair-display-700-latin.woff2") format("woff2");
  unicode-range: U+0000-00FF, U+2013-2014, U+2018-201A, U+201C-201E;
}
@font-face {
  font-family: "Playfair Display";
  font-weight: 700;
  font-display: swap;
  src: url("../fonts/playfair-display-700-latin-ext.woff2") format("woff2");
  unicode-range: U+0100-02BA, U+1E00-1EFF, U+0218-021B;
}
@font-face {
  font-family: "Source Sans 3";
  font-weight: 400;
  font-display: swap;
  src: url("../fonts/source-sans-3-400-latin.woff2") format("woff2");
  unicode-range: U+0000-00FF, U+2013-2014, U+2018-201A, U+201C-201E;
}
@font-face {
  font-family: "Source Sans 3";
  font-weight: 400;
  font-display: swap;
  src: url("../fonts/source-sans-3-400-latin-ext.woff2") format("woff2");
  unicode-range: U+0100-02BA, U+1E00-1EFF, U+0218-021B;
}
@font-face {
  font-family: "Source Sans 3";
  font-weight: 600;
  font-display: swap;
  src: url("../fonts/source-sans-3-600-latin.woff2") format("woff2");
  unicode-range: U+0000-00FF, U+2013-2014, U+2018-201A, U+201C-201E;
}
@font-face {
  font-family: "Source Sans 3";
  font-weight: 600;
  font-display: swap;
  src: url("../fonts/source-sans-3-600-latin-ext.woff2") format("woff2");
  unicode-range: U+0100-02BA, U+1E00-1EFF, U+0218-021B;
}

:root {
  --navy: #1c2b4a;
  --navy-dark: #14203a;
  --cream: #faf7f2;
  --gold: #b08d3e;
  --gold-light: #c9a862;
  --ink: #2a2a2a;
  --serif: "Playfair Display", Georgia, "Times New Roman", serif;
  --sans: "Source Sans 3", "Helvetica Neue", Arial, sans-serif;
}

* { margin: 0; padding: 0; box-sizing: border-box; }

html { scroll-behavior: smooth; }

body {
  font-family: var(--sans);
  font-size: 1.0625rem;
  line-height: 1.65;
  color: var(--ink);
  background: var(--cream);
}

h1, h2, h3 { font-family: var(--serif); font-weight: 700; line-height: 1.2; }

img { max-width: 100%; display: block; }

a { color: var(--gold); }

/* Header / nav */
.site-header {
  position: sticky;
  top: 0;
  z-index: 10;
  background: var(--navy);
  border-bottom: 2px solid var(--gold);
}
.nav {
  max-width: 68rem;
  margin: 0 auto;
  padding: 0.75rem 1.25rem;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
  flex-wrap: wrap;
}
.nav-brand {
  font-family: var(--serif);
  font-weight: 700;
  font-size: 1.05rem;
  color: var(--cream);
  text-decoration: none;
  white-space: nowrap;
}
.nav-links {
  list-style: none;
  display: flex;
  gap: 1.25rem;
  align-items: center;
  flex-wrap: wrap;
}
.nav-links a {
  color: var(--cream);
  text-decoration: none;
  font-size: 0.95rem;
  letter-spacing: 0.02em;
}
.nav-links a:hover { color: var(--gold-light); }
.lang-switch {
  border: 1px solid var(--gold);
  border-radius: 3px;
  padding: 0.15rem 0.55rem;
  font-weight: 600;
}

/* Hero */
.hero {
  background: linear-gradient(160deg, var(--navy) 0%, var(--navy-dark) 100%);
  color: var(--cream);
  text-align: center;
  padding: 6rem 1.25rem 5.5rem;
}
.hero-inner { max-width: 46rem; margin: 0 auto; }
.hero h1 { font-size: clamp(2.1rem, 5vw, 3.25rem); margin-bottom: 0.75rem; }
.hero .tagline {
  font-size: 1.2rem;
  letter-spacing: 0.06em;
  color: var(--gold-light);
  margin-bottom: 2.25rem;
}
.hero .rule {
  width: 4rem;
  height: 2px;
  background: var(--gold);
  margin: 1.5rem auto;
  border: 0;
}

.btn {
  display: inline-block;
  background: var(--gold);
  color: var(--navy-dark);
  font-weight: 600;
  text-decoration: none;
  padding: 0.8rem 2.2rem;
  border-radius: 3px;
  border: 0;
  font-size: 1.05rem;
  font-family: var(--sans);
  cursor: pointer;
}
.btn:hover { background: var(--gold-light); }

/* Sections */
.section { padding: 4.5rem 1.25rem; }
.section > .inner { max-width: 68rem; margin: 0 auto; }
.section-title {
  font-size: clamp(1.6rem, 3.5vw, 2.2rem);
  text-align: center;
  color: var(--navy);
  margin-bottom: 0.75rem;
}
.section-title + .rule {
  width: 3.5rem;
  height: 2px;
  background: var(--gold);
  margin: 0 auto 2.75rem;
  border: 0;
}
.section--navy { background: var(--navy); color: var(--cream); }
.section--navy .section-title { color: var(--cream); }

/* About */
.about-grid {
  display: grid;
  grid-template-columns: 220px 1fr;
  gap: 2.5rem;
  align-items: start;
  max-width: 56rem;
  margin: 0 auto;
}
.portrait {
  border: 3px solid var(--gold);
  border-radius: 3px;
}
.about-grid p + p { margin-top: 1rem; }

/* Practice-area cards */
.cards {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1.5rem;
}
.card {
  background: var(--cream);
  color: var(--ink);
  border-top: 3px solid var(--gold);
  border-radius: 3px;
  padding: 2rem 1.5rem;
}
.card h3 { color: var(--navy); font-size: 1.25rem; margin-bottom: 0.75rem; }

/* Contact */
.contact-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 3rem;
  max-width: 56rem;
  margin: 0 auto;
}
.contact-form label {
  display: block;
  font-weight: 600;
  margin: 1rem 0 0.25rem;
  color: var(--navy);
}
.contact-form input,
.contact-form textarea {
  width: 100%;
  padding: 0.65rem 0.75rem;
  border: 1px solid #c9c2b4;
  border-radius: 3px;
  font: inherit;
  background: #fff;
}
.contact-form input:focus,
.contact-form textarea:focus { outline: 2px solid var(--gold); border-color: var(--gold); }
.contact-form .btn { margin-top: 1.5rem; }
.contact-info h3 { color: var(--navy); font-size: 1.2rem; margin: 1.25rem 0 0.25rem; }
.contact-info p a { color: var(--navy); }

/* Footer */
.site-footer {
  background: var(--navy-dark);
  color: #b9c0cf;
  text-align: center;
  font-size: 0.9rem;
  padding: 1.5rem 1.25rem;
}

/* Mobile */
@media (max-width: 44rem) {
  .nav { justify-content: center; }
  .about-grid { grid-template-columns: 1fr; }
  .portrait { max-width: 220px; margin: 0 auto; }
  .cards { grid-template-columns: 1fr; }
  .contact-grid { grid-template-columns: 1fr; }
  .hero { padding: 4rem 1.25rem 3.5rem; }
}
```

- [ ] **Step 2: Verify the CSS references match the font files on disk**

Run:
```bash
cd /Users/alexr/publicrepos/avocat-bianca-rohozneanu
grep -o '\.\./fonts/[a-z0-9-]*\.woff2' css/style.css | sort -u | sed 's|\.\./||' | while read f; do [ -f "$f" ] && echo "OK $f" || echo "MISSING $f"; done
```
Expected: six `OK` lines, zero `MISSING`.

- [ ] **Step 3: Commit**

```bash
git add css/style.css
git commit -m "feat: add shared stylesheet with local font faces and classic navy/gold theme"
```

---

### Task 3: Romanian homepage

**Files:**
- Create: `img/portret-placeholder.svg`
- Create: `index.html`

**Interfaces:**
- Consumes: `css/style.css` class names from Task 2; `img/portret-placeholder.svg`.
- Produces: `index.html` — the RO page. Task 4 mirrors its structure exactly and links back to `/` via the language switcher. The Netlify form is named `contact`; Task 4 MUST reuse the same form name.

- [ ] **Step 1: Create the portrait placeholder SVG**

```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 220 260" role="img" aria-label="Portret">
  <rect width="220" height="260" fill="#1c2b4a"/>
  <circle cx="110" cy="95" r="42" fill="#b08d3e" opacity="0.85"/>
  <path d="M40 240 Q40 165 110 165 Q180 165 180 240 Z" fill="#b08d3e" opacity="0.85"/>
  <text x="110" y="252" text-anchor="middle" font-family="Georgia, serif" font-size="11" fill="#faf7f2">Foto — PROVIZORIU</text>
</svg>
```

- [ ] **Step 2: Write index.html (complete file)**

```html
<!DOCTYPE html>
<html lang="ro">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Avocat Bianca Rohozneanu — Cabinet de avocatură Cluj-Napoca</title>
  <meta name="description" content="Avocat Bianca Rohozneanu — consultanță și reprezentare juridică în drept civil, drept comercial și dreptul familiei, în Cluj-Napoca.">
  <link rel="alternate" hreflang="ro" href="/">
  <link rel="alternate" hreflang="en" href="/en/">
  <link rel="stylesheet" href="css/style.css">
</head>
<body>

<header class="site-header">
  <nav class="nav" aria-label="Navigare principală">
    <a class="nav-brand" href="#top">Av. Bianca Rohozneanu</a>
    <ul class="nav-links">
      <li><a href="#despre">Despre</a></li>
      <li><a href="#domenii">Domenii de practică</a></li>
      <li><a href="#contact">Contact</a></li>
      <li><a class="lang-switch" href="/en/" lang="en" hreflang="en">EN</a></li>
    </ul>
  </nav>
</header>

<main id="top">

  <section class="hero">
    <div class="hero-inner">
      <h1>Avocat Bianca Rohozneanu</h1>
      <hr class="rule">
      <p class="tagline">Cabinet de avocatură · Cluj-Napoca</p>
      <a class="btn" href="#contact">Contactează-mă</a>
    </div>
  </section>

  <section id="despre" class="section">
    <div class="inner">
      <h2 class="section-title">Despre</h2>
      <hr class="rule">
      <div class="about-grid">
        <!-- PROVIZORIU -->
        <img class="portrait" src="img/portret-placeholder.svg" alt="Portret Bianca Rohozneanu" width="220" height="260">
        <div>
          <!-- PROVIZORIU -->
          <p>Bianca Rohozneanu este avocat în Cluj-Napoca, membră a Baroului Cluj, și oferă consultanță juridică și reprezentare în fața instanțelor de judecată persoanelor fizice și companiilor.</p>
          <!-- PROVIZORIU -->
          <p>Abordarea sa pune accent pe claritate, onestitate și soluții practice: fiecare client primește o evaluare realistă a situației și o strategie adaptată obiectivelor sale.</p>
        </div>
      </div>
    </div>
  </section>

  <section id="domenii" class="section section--navy">
    <div class="inner">
      <h2 class="section-title">Domenii de practică</h2>
      <hr class="rule">
      <div class="cards">
        <article class="card">
          <h3>Drept civil</h3>
          <!-- PROVIZORIU -->
          <p>Consultanță și reprezentare în litigii civile: contracte, răspundere civilă, proprietate, revendicări și recuperări de creanțe.</p>
        </article>
        <article class="card">
          <h3>Drept comercial</h3>
          <!-- PROVIZORIU -->
          <p>Asistență juridică pentru societăți: redactarea și negocierea contractelor comerciale, litigii între profesioniști, relații cu partenerii de afaceri.</p>
        </article>
        <article class="card">
          <h3>Dreptul familiei</h3>
          <!-- PROVIZORIU -->
          <p>Divorț, exercitarea autorității părintești, pensie de întreținere și partaj — situații sensibile, tratate cu discreție și empatie.</p>
        </article>
      </div>
    </div>
  </section>

  <section id="contact" class="section">
    <div class="inner">
      <h2 class="section-title">Contact</h2>
      <hr class="rule">
      <div class="contact-grid">
        <form class="contact-form" name="contact" method="POST" data-netlify="true">
          <input type="hidden" name="form-name" value="contact">
          <label for="nume">Nume</label>
          <input id="nume" name="nume" type="text" required autocomplete="name">
          <label for="email">Email</label>
          <input id="email" name="email" type="email" required autocomplete="email">
          <label for="mesaj">Mesaj</label>
          <textarea id="mesaj" name="mesaj" rows="6" required></textarea>
          <button class="btn" type="submit">Trimite mesajul</button>
        </form>
        <div class="contact-info">
          <h3>Telefon</h3>
          <!-- PROVIZORIU -->
          <p><a href="tel:+40700000000">+40 700 000 000</a></p>
          <h3>Email</h3>
          <!-- PROVIZORIU -->
          <p><a href="mailto:contact@example.com">contact@example.com</a></p>
          <h3>Adresă</h3>
          <!-- PROVIZORIU -->
          <p>Str. Exemplu nr. 1<br>Cluj-Napoca, România</p>
        </div>
      </div>
    </div>
  </section>

</main>

<footer class="site-footer">
  <!-- PROVIZORIU -->
  <p>© 2026 Avocat Bianca Rohozneanu · Baroul Cluj</p>
</footer>

</body>
</html>
```

- [ ] **Step 3: Verify the page serves and placeholders are marked**

Run:
```bash
cd /Users/alexr/publicrepos/avocat-bianca-rohozneanu
python3 -m http.server 8321 >/dev/null 2>&1 &
sleep 1
curl -s -o /dev/null -w "index: %{http_code}\n" http://localhost:8321/
curl -s -o /dev/null -w "css: %{http_code}\n" http://localhost:8321/css/style.css
curl -s -o /dev/null -w "svg: %{http_code}\n" http://localhost:8321/img/portret-placeholder.svg
grep -c 'PROVIZORIU' index.html
grep -c 'data-netlify="true"' index.html
kill %1
```
Expected: three `200` lines; `PROVIZORIU` count ≥ 8; `data-netlify` count = 1.

- [ ] **Step 4: Commit**

```bash
git add index.html img/portret-placeholder.svg
git commit -m "feat: add Romanian homepage with hero, about, practice areas and Netlify contact form"
```

---

### Task 4: English page

**Files:**
- Create: `en/index.html`

**Interfaces:**
- Consumes: `css/style.css` (as `../css/style.css`), `img/portret-placeholder.svg` (as `../img/…`), structure of Task 3's `index.html`. Form name `contact` (same Netlify form; field names stay `nume`/`email`/`mesaj` so submissions share one inbox — only labels are translated).
- Produces: `en/index.html`; the RO page's `EN` switcher (already pointing at `/en/`) now resolves.

- [ ] **Step 1: Write en/index.html (complete file)**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Bianca Rohozneanu, Attorney at Law — Cluj-Napoca, Romania</title>
  <meta name="description" content="Bianca Rohozneanu, attorney at law in Cluj-Napoca — legal advice and representation in civil law, commercial law and family law.">
  <link rel="alternate" hreflang="ro" href="/">
  <link rel="alternate" hreflang="en" href="/en/">
  <link rel="stylesheet" href="../css/style.css">
</head>
<body>

<header class="site-header">
  <nav class="nav" aria-label="Main navigation">
    <a class="nav-brand" href="#top">Bianca Rohozneanu</a>
    <ul class="nav-links">
      <li><a href="#about">About</a></li>
      <li><a href="#practice">Practice areas</a></li>
      <li><a href="#contact">Contact</a></li>
      <li><a class="lang-switch" href="/" lang="ro" hreflang="ro">RO</a></li>
    </ul>
  </nav>
</header>

<main id="top">

  <section class="hero">
    <div class="hero-inner">
      <h1>Bianca Rohozneanu</h1>
      <hr class="rule">
      <p class="tagline">Attorney at Law · Cluj-Napoca, Romania</p>
      <a class="btn" href="#contact">Get in touch</a>
    </div>
  </section>

  <section id="about" class="section">
    <div class="inner">
      <h2 class="section-title">About</h2>
      <hr class="rule">
      <div class="about-grid">
        <!-- PROVIZORIU -->
        <img class="portrait" src="../img/portret-placeholder.svg" alt="Portrait of Bianca Rohozneanu" width="220" height="260">
        <div>
          <!-- PROVIZORIU -->
          <p>Bianca Rohozneanu is an attorney based in Cluj-Napoca, a member of the Cluj Bar Association, providing legal advice and court representation to individuals and businesses.</p>
          <!-- PROVIZORIU -->
          <p>Her approach is built on clarity, honesty and practical solutions: every client receives a realistic assessment of their situation and a strategy tailored to their goals.</p>
        </div>
      </div>
    </div>
  </section>

  <section id="practice" class="section section--navy">
    <div class="inner">
      <h2 class="section-title">Practice areas</h2>
      <hr class="rule">
      <div class="cards">
        <article class="card">
          <h3>Civil law</h3>
          <!-- PROVIZORIU -->
          <p>Advice and representation in civil disputes: contracts, civil liability, property matters and debt recovery.</p>
        </article>
        <article class="card">
          <h3>Commercial law</h3>
          <!-- PROVIZORIU -->
          <p>Legal support for companies: drafting and negotiating commercial contracts, disputes between businesses, and day-to-day business relations.</p>
        </article>
        <article class="card">
          <h3>Family law</h3>
          <!-- PROVIZORIU -->
          <p>Divorce, child custody, child support and division of property — sensitive matters handled with discretion and empathy.</p>
        </article>
      </div>
    </div>
  </section>

  <section id="contact" class="section">
    <div class="inner">
      <h2 class="section-title">Contact</h2>
      <hr class="rule">
      <div class="contact-grid">
        <form class="contact-form" name="contact" method="POST" data-netlify="true">
          <input type="hidden" name="form-name" value="contact">
          <label for="nume">Name</label>
          <input id="nume" name="nume" type="text" required autocomplete="name">
          <label for="email">Email</label>
          <input id="email" name="email" type="email" required autocomplete="email">
          <label for="mesaj">Message</label>
          <textarea id="mesaj" name="mesaj" rows="6" required></textarea>
          <button class="btn" type="submit">Send message</button>
        </form>
        <div class="contact-info">
          <h3>Phone</h3>
          <!-- PROVIZORIU -->
          <p><a href="tel:+40700000000">+40 700 000 000</a></p>
          <h3>Email</h3>
          <!-- PROVIZORIU -->
          <p><a href="mailto:contact@example.com">contact@example.com</a></p>
          <h3>Address</h3>
          <!-- PROVIZORIU -->
          <p>Str. Exemplu nr. 1<br>Cluj-Napoca, Romania</p>
        </div>
      </div>
    </div>
  </section>

</main>

<footer class="site-footer">
  <!-- PROVIZORIU -->
  <p>© 2026 Bianca Rohozneanu, Attorney at Law · Cluj Bar Association</p>
</footer>

</body>
</html>
```

- [ ] **Step 2: Verify both pages serve and cross-link correctly**

Run:
```bash
cd /Users/alexr/publicrepos/avocat-bianca-rohozneanu
python3 -m http.server 8321 >/dev/null 2>&1 &
sleep 1
curl -s -o /dev/null -w "ro: %{http_code}\n" http://localhost:8321/
curl -s -o /dev/null -w "en: %{http_code}\n" http://localhost:8321/en/
grep -c 'hreflang' index.html en/index.html
grep -c 'href="/en/"' index.html
grep -c 'href="/"' en/index.html
kill %1
```
Expected: both `200`; hreflang count ≥ 3 per file; the last two greps ≥ 1 each.

- [ ] **Step 3: Commit**

```bash
git add en/index.html
git commit -m "feat: add English version of the site"
```

---

### Task 5: README and local visual review

**Files:**
- Create: `README.md`

**Interfaces:**
- Consumes: the finished site (Tasks 1–4).
- Produces: `README.md`; a human visual check in the browser (this step is interactive with Alex).

- [ ] **Step 1: Write README.md**

````markdown
# Avocat Bianca Rohozneanu — site de prezentare

Site static bilingv (RO/EN): HTML + CSS, fără build, fără JavaScript.

## Structură

- `index.html` — pagina în română (implicită)
- `en/index.html` — pagina în engleză
- `css/style.css` — stilurile comune
- `fonts/` — fonturi servite local (Playfair Display, Source Sans 3)
- `img/` — imagini

## Rulare locală

```bash
python3 -m http.server 8321
# apoi deschide http://localhost:8321/
```

## Cum actualizezi conținutul

Tot textul provizoriu este marcat în HTML cu comentariul `<!-- PROVIZORIU -->`
pe rândul de deasupra. Caută `PROVIZORIU` în `index.html` și `en/index.html`
și înlocuiește textul de sub fiecare marcaj (biografie, telefon, email,
adresă, poză). După înlocuire, șterge comentariul.

Poza de profil: înlocuiește `img/portret-placeholder.svg` cu o fotografie
reală (ex. `img/portret.jpg`, ~440×520px) și actualizează atributul `src`
în ambele pagini.

## Publicare

Găzduit pe Netlify, conectat la acest repo — orice push pe `main` publică
automat. Formularul de contact folosește Netlify Forms; mesajele apar în
panoul Netlify → Forms (se poate configura notificare pe email).
````

- [ ] **Step 2: Open the site in the browser for Alex to review**

Run:
```bash
cd /Users/alexr/publicrepos/avocat-bianca-rohozneanu
python3 -m http.server 8321 >/dev/null 2>&1 &
open http://localhost:8321/
```
Then STOP and ask Alex (in Romanian) to review both pages (RO and EN, desktop width and narrow window for mobile). Iterate on feedback before continuing. Kill the server when done: `kill %1`.

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs: add README with editing and deployment instructions"
```

---

### Task 6: Publish — GitHub repo + Netlify

This task is semi-interactive: repo creation needs Alex's GitHub account, and the Netlify connection happens in the browser.

**Files:**
- No new files (push + hosted-service configuration only).

**Interfaces:**
- Consumes: the complete committed site.
- Produces: public repo `github.com/alexrohozneanu/avocat-bianca-rohozneanu`, live site at `https://<site-name>.netlify.app`.

- [ ] **Step 1: Create the GitHub repository**

Preferred: install GitHub CLI and create the repo in one step:
```bash
brew install gh
gh auth login        # interactive — Alex runs this (suggest `! gh auth login` in the prompt)
cd /Users/alexr/publicrepos/avocat-bianca-rohozneanu
gh repo create avocat-bianca-rohozneanu --public --source=. --push
```
Fallback (no gh): Alex creates an empty public repo named `avocat-bianca-rohozneanu` at github.com/new, then:
```bash
cd /Users/alexr/publicrepos/avocat-bianca-rohozneanu
git remote add origin git@github.com:alexrohozneanu/avocat-bianca-rohozneanu.git
git push -u origin main
```

- [ ] **Step 2: Verify the push**

Run: `git ls-remote origin main`
Expected: one line with the latest commit hash matching `git rev-parse main`.

- [ ] **Step 3: Connect Netlify (Alex, in the browser — guide him through it)**

1. Log in / sign up at app.netlify.com (can use the GitHub account).
2. "Add new site" → "Import an existing project" → GitHub → select `avocat-bianca-rohozneanu`.
3. Build settings: leave **build command empty**, publish directory = repo root (`/`). Deploy.
4. In Site configuration → Forms: enable form detection, redeploy if prompted.
5. Optionally rename the site (Site configuration → Site details → Change site name) to e.g. `avocat-rohozneanu` → `https://avocat-rohozneanu.netlify.app`.

- [ ] **Step 4: Verify the live site**

Run:
```bash
curl -s -o /dev/null -w "ro: %{http_code}\n" https://<site-name>.netlify.app/
curl -s -o /dev/null -w "en: %{http_code}\n" https://<site-name>.netlify.app/en/
```
Expected: both `200`. Then submit a test message through the live contact form and confirm it appears under Netlify → Forms.

- [ ] **Step 5: Done — note follow-ups**

Report to Alex the live URL and the remaining follow-ups (outside this plan): custom domain (`rohozneanu.ro` / `avocatrohozneanu.ro`), replacing PROVIZORIU content after talking to Bianca, Google Business profile.
